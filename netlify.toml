[build]
  command = "npm run build"
  publish = ".next"

# Default public Supabase target for this CRM (Kalyani / Radhaswami data).
# If Netlify UI has different NEXT_PUBLIC_* values, update/remove those UI vars
# so this project is used, then Clear cache and deploy.
[build.environment]
  NEXT_PUBLIC_SUPABASE_URL = "https://pelwnhukierrqienpveb.supabase.co"

[functions]
  directory = "netlify/functions"

# Also keep pack copies at /api/*.js for reference / alternate mounts.
